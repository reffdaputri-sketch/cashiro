<UserControl x:Class="KioslyDesktop.Views.PosView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" Text="POS / Kasir" FontSize="28" FontWeight="Bold" Foreground="#333333"/>
        
        <Grid Grid.Row="1" Margin="0,20,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="350"/>
            </Grid.ColumnDefinitions>

            <!-- Product Catalog & Scanner Input -->
            <Border Grid.Column="0" Background="White" CornerRadius="10" Margin="0,0,20,0" Padding="20">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <!-- Scanner Wedge Focus Box -->
                    <TextBox Grid.Row="0" FontSize="18" Padding="10" Background="#F5F5F5" BorderThickness="0" 
                             Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}" 
                             ToolTip="Scan Barcode atau Cari Produk..."/>
                    
                    <TextBlock Grid.Row="1" Text="Katalog Produk" FontSize="18" FontWeight="SemiBold" Foreground="Gray" Margin="0,20,0,10"/>
                    
                    <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
                        <ItemsControl ItemsSource="{Binding Products}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Button Command="{Binding DataContext.AddToCartCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                            CommandParameter="{Binding}" Background="Transparent" BorderThickness="0" Margin="0,0,15,15">
                                        <Border Background="#F9F9F9" Width="150" Height="150" CornerRadius="8">
                                            <StackPanel VerticalAlignment="Center">
                                                <TextBlock Text="{Binding Name}" FontWeight="Bold" TextAlignment="Center" FontSize="16" TextWrapping="Wrap"/>
                                                <TextBlock Text="{Binding Price, StringFormat=Rp {0:N0}}" TextAlignment="Center" Foreground="#007BFF" Margin="0,5,0,0"/>
                                                <TextBlock Text="{Binding Stock, StringFormat=Stok: {0}}" TextAlignment="Center" Foreground="Gray" Margin="0,5,0,0" FontSize="12"/>
                                            </StackPanel>
                                        </Border>
                                    </Button>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Grid>
            </Border>

            <!-- Cart Summary -->
            <Border Grid.Column="1" Background="White" CornerRadius="10" Padding="20">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="Daftar Pesanan" FontSize="20" FontWeight="Bold" Foreground="#333333"/>
                    
                    <!-- List -->
                    <ListView Grid.Row="1" Margin="0,20,0,0" ItemsSource="{Binding Cart}" BorderThickness="0">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="0,5">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0">
                                        <TextBlock Text="{Binding Product.Name}" FontWeight="Bold"/>
                                        <TextBlock Text="{Binding Product.Price, StringFormat=Rp {0:N0}}" Foreground="Gray"/>
                                    </StackPanel>
                                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                                        <TextBlock Text="x" Margin="0,0,5,0"/>
                                        <TextBlock Text="{Binding Quantity}" FontWeight="Bold" FontSize="16"/>
                                    </StackPanel>
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>

                    <!-- Total & Action -->
                    <StackPanel Grid.Row="2">
                        <Separator Margin="0,10"/>
                        <Grid>
                            <TextBlock Text="Total:" FontSize="24" FontWeight="Bold"/>
                            <TextBlock Text="{Binding TotalAmount, StringFormat=Rp {0:N0}}" FontSize="24" FontWeight="Bold" Foreground="#007BFF" HorizontalAlignment="Right"/>
                        </Grid>
                        <Button Content="BAYAR" Command="{Binding ProcessPaymentCommand}" Background="#007BFF" Height="50" FontSize="20" FontWeight="Bold" Margin="0,20,0,0" Foreground="White"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
